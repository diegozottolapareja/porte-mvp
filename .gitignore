# Dependencias — el proyecto usa pnpm (pnpm-lock.yaml es la fuente de verdad
# para Vercel); un package-lock.json desincronizado rompe el build ahí.
node_modules
.pnpm-store
package-lock.json

# Compilación y caché
dist
.vite
.eslintcache

# Variables de entorno locales
.env
.env.local
.env.*.local

# Archivos del sistema
.DS_Store
Thumbs.db
.vercel
.env*

# Cache local de la CLI de Supabase (link al proyecto, versiones, etc.)
supabase/.temp

# Logs/snapshots de sesiones de Playwright MCP
.playwright-mcp
